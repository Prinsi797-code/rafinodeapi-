apt install -y python3 python3-pip
pip3 install playwright
playwright install chromium
playwright install-deps

this command run in system after 
1. session.json remove 
2. python3 ig_login_bot.py 
3. node server.js
